apiVersion: v1
kind: Service
metadata:
  name: {{ include "galyarder-design.fullname" . }}
  labels:
    {{- include "galyarder-design.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "galyarder-design.selectorLabels" . | nindent 4 }}
