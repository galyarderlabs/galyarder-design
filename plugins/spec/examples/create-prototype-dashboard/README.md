# Prototype Dashboard

This is the full reference example for a spec create plugin. It includes a portable `SKILL.md`, GD sidecar, preview, seed asset, and eval file.

```bash
gd plugin validate .
gd plugin install .
gd plugin apply create-prototype-dashboard --input audience="support leads" --input topic="enterprise onboarding risk"
```

